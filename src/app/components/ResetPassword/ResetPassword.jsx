"use client";
import React, { useContext } from 'react'
import { authContext } from '../../../Providers/AuthProvider/Auth.js';

const ResetPassword = () => {
    const {handleResetPasswordFun} = useContext(authContext);
  return <>
  
  </>
}

export default ResetPassword
